services:
  - type: web
    name: amazon-design-schedule
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /var/data
    disk:
      name: schedule-data
      mountPath: /var/data
      sizeGB: 1
